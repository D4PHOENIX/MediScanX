# =============================================================================
# MediScanX — ecg_service CI/CD Pipeline
# =============================================================================
# Trigger : Pull Requests to `dev` or `main` branch touching any file under backend/ecg_service/**
# Runner  : ubuntu-latest
# Toolchain: uv (astral-sh) with optimal PyTorch CPU wheel caching
# Jobs    : 1. test        — mocked pytest suite (required gate to merge)
#           2. docker-build — multi-stage Dockerfile dry-run (no push)
#
# Mocking strategy
# --------------------------------------
# The model files are loaded during initialization, guarded by a
# try/except that falls back cleanly.
# In CI we set ECG_ONNX_PATH and ECG_CKPT_PATH to a /dev/null/... path so that
# Path(model_path) does not exist → clean fallback, no disk I/O,
# no mocked weights needed.
#
# PyTorch caching strategy
# ------------------------
# torchvision + torch CPU wheels are large (~500 MB combined).  We cache
# via astral-sh/setup-uv keyed on uv.lock.  Unchanged lockfile = warm
# restore in seconds.  The [tool.uv.sources] pytorch-cpu index in
# pyproject.toml pins CPU-only builds.
# =============================================================================

name: ecg-service CI

on:
    pull_request:
        branches:
            - dev
            - main
        paths:
            - "backend/ecg_service/**"

# Cancel any superseded run for the same PR branch immediately.
concurrency:
    group: ecg-service-${{ github.head_ref }}
    cancel-in-progress: true

env:
    UV_VERSION: "0.5.18"
    PYTHON_VERSION: "3.11"
    IMAGE_NAME: mediscanx/ecg-service

# =============================================================================
# JOB 1 — Test
# =============================================================================
jobs:
    test:
        name: "Pytest Suite (mocked weights + thresholds)"
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: backend/ecg_service

        env:
            # -----------------------------------------------------------------------
            # /dev/null/... paths guarantee:
            #   - Path.exists() → False  (no FileNotFoundError from missing mounts)
            #   - Engine initialization falls back gracefully
            #   - No physical .pth or .onnx files need to be present in the runner
            # -----------------------------------------------------------------------
            ECG_ONNX_PATH: "/dev/null/mock_onnx.onnx"
            ECG_CKPT_PATH: "/dev/null/mock_ckpt.ckpt"

        steps:
            # -----------------------------------------------------------------------
            - name: Checkout repository
              uses: actions/checkout@v4

            # -----------------------------------------------------------------------
            # Dual-layer PyTorch caching:
            #   1. Wheel download cache (~/.cache/uv) keyed on uv.lock hash
            #   2. Virtualenv restore when lock is unchanged
            # pytorch-cpu index pins CPU-only torch + torchvision (~500 MB total).
            - name: Install uv ${{ env.UV_VERSION }}
              uses: astral-sh/setup-uv@v5
              with:
                  version: ${{ env.UV_VERSION }}
                  enable-cache: true
                  cache-dependency-glob: "backend/ecg_service/uv.lock"

            # -----------------------------------------------------------------------
            - name: Set up Python ${{ env.PYTHON_VERSION }}
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ env.PYTHON_VERSION }}

            # -----------------------------------------------------------------------
            - name: Network Diagnostics
              run: |
                  echo "Testing PyPI connectivity..."
                  curl -I -v -m 10 https://pypi.org || echo "PyPI unreachable"
                  echo "Testing GitHub connectivity..."
                  curl -I -v -m 10 https://github.com || echo "GitHub unreachable"

            # -----------------------------------------------------------------------
            # --frozen prevents lock-file mutation (reproducible CI builds).
            # Dev extras supply pytest + pytest-asyncio + httpx.
            # Hardened against transient drops with UV_HTTP_TIMEOUT and shell retries.
            - name: Install dependencies (uv sync --frozen)
              env:
                  UV_HTTP_TIMEOUT: "120"
              run: |
                  for i in {1..5}; do
                      echo "Attempt $i to sync dependencies..."
                      if uv sync --frozen --all-packages --all-extras; then
                          echo "Dependencies synced successfully."
                          break
                      else
                          echo "uv sync failed. Retrying in 10s..."
                          sleep 10
                      fi
                  done

            # -----------------------------------------------------------------------
            - name: Run pytest
              run: |
                  uv run pytest tests/ \
                    --tb=short \
                    --strict-markers \
                    -v \
                    --color=yes \
                    --junitxml=pytest-results.xml

            # -----------------------------------------------------------------------
            - name: Upload pytest results
              if: always() && !env.ACT
              uses: actions/upload-artifact@v4
              with:
                  name: ecg-pytest-results-${{ github.run_number }}
                  path: backend/ecg_service/pytest-results.xml
                  retention-days: 7

    # =============================================================================
    # JOB 2 — Docker dry-run build
    # =============================================================================
    docker-build:
        name: "Docker Build (dry-run, no push)"
        runs-on: ubuntu-latest
        needs: test

        steps:
            # -----------------------------------------------------------------------
            - name: Checkout repository
              uses: actions/checkout@v4

            # -----------------------------------------------------------------------
            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            # -----------------------------------------------------------------------
            # Full multi-stage build without pushing.
            # --load exports to the local daemon so the smoke test below can run.
            # --no-cache exercises every layer on every PR (catches stale COPY paths).
            - name: Build ecg_service Docker image
              uses: docker/build-push-action@v6
              with:
                  context: ./backend/ecg_service
                  file: ./backend/ecg_service/Dockerfile
                  push: false
                  load: true
                  no-cache: true
                  tags: ${{ env.IMAGE_NAME }}:pr-${{ github.event.pull_request.number }}
                  labels: |
                      org.opencontainers.image.revision=${{ github.sha }}
                      org.opencontainers.image.source=${{ github.repository }}

            # -----------------------------------------------------------------------
            # Smoke-test: verify the full Python import chain resolves inside the
            # runtime image without starting the ASGI server.  This catches:
            #   - Dockerfile COPY path regressions (Bug #10 pattern)
            #   - Missing dependencies not caught at build time
            #   - Absence of print() calls in __post_init__ (Bug #2 regression)
            - name: Smoke-test image import chain
              run: |
                  docker run --rm \
                    -e ECG_ONNX_PATH=/dev/null/mock_onnx.onnx \
                    -e ECG_CKPT_PATH=/dev/null/mock_ckpt.ckpt \
                    ${{ env.IMAGE_NAME }}:pr-${{ github.event.pull_request.number }} \
                    python -c "import sys, io, contextlib; sys.path.insert(0, '/app/src'); from app.core.config import Settings; from app.core.exceptions import ExceptionRegistry; captured = io.StringIO(); ctx = contextlib.redirect_stdout(captured); ctx.__enter__(); cfg = Settings(); ctx.__exit__(None, None, None); assert captured.getvalue() == '', f'__post_init__ still calls print(): {captured.getvalue()!r}'; assert cfg.num_classes == 5, f'Wrong num_classes: {cfg.num_classes}'; assert cfg.num_leads == 12; print('Import OK — Settings, ExceptionRegistry load cleanly.'); print(f'num_classes={cfg.num_classes}  num_leads={cfg.num_leads}'); print('No print() calls detected in __post_init__ (Bug #2 regression check passed.')"
